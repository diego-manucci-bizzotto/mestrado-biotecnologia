# Backend

FastAPI para processar FASTA e escanear motifs em pipeline PWM-only.

## Comandos

```powershell
cd backend
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
# Run from /backend
python -m uvicorn app.main:app --reload
```
